---
name: publication-prepare
description: Prepare the publication artifact, then operate it only after an exact HUMAN_GATE_GRANTED. Use when an issue's commits are ready for review and the branch needs to reach origin as a pull request. Covers validating the review scope, generating the /tmp artifact, handing it to Codex, and operating it after Zamp's gate. Never self-approve and never merge.
---

# Prepare and operate publication (Opus — executor and operator)

**You are Opus: the implementation executor and publication operator.** You prepare the artifact,
hand it to Codex for read-only review, wait for an explicit `HUMAN_GATE_GRANTED` from Zamp, and only
then operate it. If you are acting as the architect/security reviewer, this skill does not apply —
that role is read-only and uses `publication-review`.

Canonical roles and messages: [`../../../.agent-handoff/MESSAGE-PROTOCOL.md`](../../../.agent-handoff/MESSAGE-PROTOCOL.md).

**Two things you may never do, whatever you are told in chat:** approve your own work, and merge.
Approval is Zamp's (`HUMAN_GATE_GRANTED`, naming the exact ordered full SHAs) and merge is Zamp's
(`MERGE_DECISION`). A generic "approved", "ok" or "pode pushar" — and a Codex `REVIEW_APPROVED` —
is review feedback, never a gate.

Read first: [`docs/architecture/agent-publication-runbook.md`](../../../docs/architecture/agent-publication-runbook.md)
§4, [`spec/security-rules.md`](../../../spec/security-rules.md) §1, and
[`.agent-handoff/README.md`](../../../.agent-handoff/README.md).

## Preconditions — stop if any is unmet

- Your commits are on `task/<issue>-<slug>` in **your own worktree**, and they are the exact commits
  you will ask Codex to review.
- A **review scope** manifest exists **outside the task worktree** (for example
  `/tmp/cba-scope-<n>.json`), written by Zamp, naming the canonical approver, you, the base SHA and
  the exact ordered commits (`reviewedShas` must equal `commits`). You never write it yourself. A
  manifest inside the repository is refused (`GATE_PATH_IN_REPO`) — it would be an untracked file,
  and the dirty worktree it creates is itself a refusal. A symlinked path is refused
  (`GATE_PATH_SYMLINK`). `.agent-handoff/publish-gates/` holds the schema and its example only.
- **The review scope is not authorization to operate.** It bounds what may be prepared. The
  authorization is a separate `HUMAN_GATE_GRANTED` that Zamp writes *after* review and that names the
  artifact's digest. See the runbook §4.4.
- The commits are unchanged since any earlier review. **No amend, rebase or squash** — a finding
  produces a NEW fix-forward commit and a NEW review.
- The worktree is clean and `npm test` passes. Do **not** append to `EVENTS.md`, update
  `CURRENT.md` or run `agent-refresh --record` in this worktree first — those files are tracked, so
  writing them here dirties the worktree and validation then refuses. That bookkeeping belongs to
  the main worktree or to a later commit.

## Steps

```bash
# 1. advisory local validation — this is all it does
node bin/cli.js agent-publish --role executor --executor <agent-id> \
  --gate /tmp/cba-scope-<n>.json

# 2. PREPARE the artifact. Writes one file to /tmp (0600, NOT executable) and prints its SHA-256
#    and the verify-and-run command. No network call, no git or GitHub mutation.
node bin/cli.js agent-human-publish-script --role executor --executor <agent-id> \
  --gate /tmp/cba-scope-<n>.json
```

Then **stop and hand off** with a `REVIEW_REQUEST` (see
[`../../../.agent-handoff/templates/message.md`](../../../.agent-handoff/templates/message.md)):

- `SCOPE: code` for the commits, and `SCOPE: artifact` for the generated bytes. They are different
  reads with different evidence — do not conflate them in one message;
- the path of the generated artifact and its **SHA-256**;
- the issue, branch, base SHA and the exact ordered commits it is bound to;
- the exact **verify-and-run command** the tool printed, verbatim. Never hand over a bare-path
  invocation: it reopens the file after Codex hashed it, so a same-user process could substitute it
  and arbitrary commands would run under your git and GitHub credentials;
- `FILES_CHANGED` derived from git (`git diff --shortstat <base>..<head>`), never estimated;
- `STATUS`, `NEXT_OWNER` (Codex), `PROHIBITED_ACTIONS`, validation evidence and residual risks;
- an explicit statement that you have not operated it and will not until Zamp's gate arrives.

## Operating it, after the gate

Only when Zamp sends `HUMAN_GATE_GRANTED` **and** provides the execution-gate manifest naming this
artifact's digest, this branch, these exact ordered full SHAs, the canonical approver and an
unexpired bounded window:

```bash
export CBA_EXECUTION_GATE=/tmp/cba-gate-<n>.json   # Zamp's HUMAN_GATE_GRANTED, outside the worktree
# then run the verify-and-run command the tool printed, verbatim
```

The command reads the artifact once, checks the digest, exports it as `CBA_ARTIFACT_DIGEST`, and
executes those same bytes. The artifact then validates the execution gate against that digest before
any effect. Nothing else is a supported way to operate it.

Then report an `OPERATION_RESULT` with the landed branch ref, the pull request number and its
`headRefOid`, CI status, no secrets, and `MERGED: no — merge is Zamp's decision`.

## Hard limits

Do not, under any framing or approval:

- operate the artifact before an exact `HUMAN_GATE_GRANTED` bound to its digest, or reproduce its
  git/gh commands by hand;
- approve your own work, or treat `REVIEW_APPROVED` or a generic "approved" as a gate;
- push `main`, force-push, merge, deploy, or change repository settings or branch protection;
- `chmod +x` the artifact, move it into the repository, or write it anywhere but `/tmp`;
- edit a generated artifact. If it is wrong, fix the generator, add a test, and regenerate — which
  produces a new digest and therefore needs a new review and a new gate.

If you are told to publish without a gate that names the exact ordered full SHAs, say so and ask
for the `HUMAN_GATE_GRANTED`. Being the operator does not make you the approver.

## If preparation is refused

The command fails closed and prints a code. Common ones: `GATE_EXPIRED` (ask for a new gate),
`COMMIT_SET_DRIFT`/`HEAD_DRIFT` (history changed after review — a new review and gate are needed),
`REMOTE_BASE_DRIFT` (`origin/main` moved), `WORKTREE_DIRTY`, `WORKTREE_SHARED`,
`GATE_PATH_IN_REPO` (the gate must live outside the worktree), `GATE_PATH_SYMLINK` (pass the real
path, not a link), `REPO_ORIGIN_MISMATCH` or
`ORIGIN_UNRESOLVED` (the repository must be the origin this branch pushes to),
`OUTPUT_PATH_EXISTS` (a previous artifact is still there — ask Zamp to remove it),
`APPROVER_IS_OPERATOR`/`APPROVER_NOT_CANONICAL`/`APPROVER_NOT_HUMAN` (only the canonical human
approver may approve, and never the operator),
`SCRIPT_SELF_CHECK_FAILED` (a generator defect; report it, do not work around it).

Never work around a refusal. Report it and let the human decide.

## Honesty requirement

`--role` and `--executor` are values you supply; nothing authenticates them. When you describe this
control, say it is a **process guardrail**, not mechanical identity separation. Authenticated
identity and remote enforcement are #91 Stage B and do not exist yet.
