{
  "name": "backstage-cba-prep-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "CBA Web MVP \u2014 deterministic learner loop, slices 1-4b (#39-#43 / #11): drill loop, mock exam, review missed + deterministic coach, persistence boundary, learner identity. Learner surface + thin route adapters over the shared BFF service (services/bff, #76). Not a root workspace.",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "test": "node --test test/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "backstage-cba-prep-bff": "file:../services/bff",
    "next": "^16.2.11",
    "oidc-client-ts": "^3.5.0",
    "react": "^19.2.7",
    "react-dom": "^19.2.7"
  }
}
